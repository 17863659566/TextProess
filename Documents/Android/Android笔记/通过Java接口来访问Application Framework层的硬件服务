1. Android 硬件抽象层(HAL)概要介绍和学习计划
  1. Android的硬件抽象层：对Linux内核驱动程序的封装，向上提供接口，屏蔽低层的实现细节。
     将对硬件的支持分为了两层
     ----------------------------------------
                  (用户空间)
                  硬件抽象层  运行在用户空间
     ----------------------------------------
                  (内核空间)
     ----------------------------------------
